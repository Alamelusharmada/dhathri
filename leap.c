#include <stdio.h>

int main()
{
    int i;
    scanf("%d",&i);
    if(i%2==0)
    {
        printf("the no is even");
    }
    else
    {
        printf("the no is odd");
    }
    return 0;
    
}
